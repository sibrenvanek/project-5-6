@ -0,0 +1,52 @@
{
    version 0.2.0,
    configurations [
        {
            name .NET Core Launch (console),
            type coreclr,
            request launch,
            preLaunchTask build,
            program ${workspaceRoot}binDebugnetcoreapp2.0P56DB2.dll,
            args [],
            cwd ${workspaceRoot},
            stopAtEntry false,
            console internalConsole
        },
        {
            name .NET Core Launch (web),
            type coreclr,
            request launch,
            preLaunchTask build,
            program ${workspaceRoot}binDebuginsert-target-framework-hereinsert-project-name-here.dll,
            args [],
            cwd ${workspaceRoot},
            stopAtEntry false,
            launchBrowser {
                enabled true,
                args ${auto-detect-url},
                windows {
                    command cmd.exe,
                    args C start ${auto-detect-url}
                },
                osx {
                    command open
                },
                linux {
                    command xdg-open
                }
            },
            env {
                ASPNETCORE_ENVIRONMENT Development
            },
            sourceFileMap {
                Views ${workspaceRoot}Views
            }
        },
        {
            name .NET Core Attach,
            type coreclr,
            request attach,
            processId ${commandpickProcess}
        }
    ]
}